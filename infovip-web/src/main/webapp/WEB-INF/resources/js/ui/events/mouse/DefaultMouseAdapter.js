/**
 * Created by attila.
 */

var DefaultMouseAdapter = easejs.Class('DefaultMouseAdapter').extend(MouseAdapter, {

    'virtual override public mouseClicked': function (eventData, Handler) {
    },

    'virtual override public mouseEntered': function (eventData, Handler) {
    },

    'virtual override public mouseExit': function (eventData, Handler) {
    },

    'virtual override public mouseMoved': function (eventData, Handler) {
    },

    'virtual override public mouseReleased': function (eventData, Handler) {
    }

});